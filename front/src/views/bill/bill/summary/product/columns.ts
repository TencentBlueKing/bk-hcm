import { costColumns } from '../../constants/columns';
import { IColumns } from '@/views/resource/resource-manage/hooks/use-columns';

export const billsProductSummaryColumns: IColumns = [...costColumns];
