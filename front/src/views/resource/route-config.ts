/**
 * @deprecated 此文件已废弃，路由已按模块拆分到各自目录下：
 * - 资源纳管 → views/resource-manage/route-config.ts
 * - 回收管理 → views/recyclebin/route-config.ts
 * - 操作记录 → views/operation-log/route-config.ts
 * - 账号管理 → views/account-manage/route-config.ts（已移至工作台菜单）
 */
import type { RouteRecordRaw } from 'vue-router';

const resourceRoutes: RouteRecordRaw[] = [];

export default resourceRoutes;
