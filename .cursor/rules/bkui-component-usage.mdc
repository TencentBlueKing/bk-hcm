---
description: bkui-vue3 组件使用规范 - 合理利用 MCP 文档确保正确使用组件
globs: ["front/**/*.vue", "front/**/*.tsx"]
alwaysApply: true
---

# bkui-vue3 组件使用规范

## 核心原则

使用 bkui-vue3 组件时，不要猜测组件能力，在适当时机主动查阅 MCP 文档。

## 必须查阅文档的场景

### 1. 配置属性选择

当需要配置组件属性时，应先查阅文档确认：
- 是否有对应的 prop 可用
- prop 的类型和可选值
- 是否有更好的内置方案，而非自己写样式覆盖

```typescript
// ❌ 错误：猜测属性存在
<bk-form-item spacing="small">  // spacing 可能不存在

// ✅ 正确：先查阅文档确认属性
// 调用 MCP: get-component { name: "form" }
```

### 2. 复杂组件使用

以下组件使用时应主动查阅：
- `bk-table` - 列配置、分页、排序等
- `bk-form` / `bk-form-item` - 表单验证、布局
- `bk-tree` - 树形数据、异步加载
- `bk-dialog` / `bk-sideslider` - 弹窗配置
- `bk-upload` - 上传配置
- `bk-select` / `bk-cascader` - 远程搜索、多选等

### 3. 不确定的用法

- 不确定某个功能是否有内置支持
- 需要自定义样式来实现某个效果时（可能有更好的方案）
- 遇到组件行为不符合预期时

### 4. 事件和插槽

- 需要监听组件事件时，确认事件名称和参数
- 需要使用插槽时，确认插槽名称和作用域

## 不需要查阅的场景

- 简单的基础组件使用（button 点击、input 绑定 v-model）
- 当前会话中已经查阅过的组件
- 对组件用法非常确定时

## 查阅方式

使用 bkui-vue3 MCP 工具：

```
// 查询单个组件
get-component { name: "form" }

// 批量查询相关组件
get-components-batch { names: ["form", "input", "select"] }

// 获取组件列表
get-component-list {}
```

## 实践建议

1. **宁可多查一次，不要猜错实现** - 查阅文档的成本远低于错误实现后修复的成本
2. **发现组件能力不足时，先确认** - 可能是你不知道而非组件不支持
3. **避免不必要的样式覆盖** - 如果需要覆盖样式实现功能，先确认是否有内置方案
