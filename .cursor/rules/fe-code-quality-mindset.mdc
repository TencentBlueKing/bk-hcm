---
description: 代码质量思维规范 - 不要盲目遵循现有代码，要思考改进
globs: ["front/**/*.vue", "front/**/*.ts", "front/**/*.tsx"]
alwaysApply: true
---

# 代码质量思维规范

## 核心原则：不要盲目遵循，要思考改进

这个项目的代码基础存在改进空间。在协作过程中，你需要：

1. **独立思考**：不要因为现有代码是某种写法就沿用，要思考是否有更好的实现
2. **主动提出**：发现更好的实现方式要**大胆提出**，重点与用户确认
3. **逐步改善**：在满足功能需求的同时，持续改进代码质量

## 代码坏味道识别

### 1. 组件一致性问题

**错误示例**：在表单中混用不同的包装元素

```vue
<!-- ❌ 错误：其他项用 bk-form-item，这里却用 p 标签 -->
<bk-form-item label="字段1">...</bk-form-item>
<p class="mt-16">
  <bk-checkbox v-model="xxx">选项</bk-checkbox>
</p>
<bk-form-item label="字段2">...</bk-form-item>
```

**正确示例**：保持组件包装的一致性

```vue
<!-- ✅ 正确：统一使用 bk-form-item -->
<bk-form-item label="字段1">...</bk-form-item>
<bk-form-item v-if="condition" property="xxx">
  <bk-checkbox v-model="xxx">选项</bk-checkbox>
</bk-form-item>
<bk-form-item label="字段2">...</bk-form-item>
```

### 2. 多余的样式代码

**问题**：为了弥补不一致的组件使用而添加额外样式

```scss
// ❌ 错误：因为用了 p 标签而需要额外的间距样式
.mt-16 {
  margin-top: 16px;
}
```

**原则**：如果需要添加样式来"修复"布局问题，先检查是否组件使用方式有问题

### 3. 组件库能力未充分利用

- `bk-form-item` 自带间距管理，不需要手动添加 margin
- `bk-form-item` 的 `label` 属性可以为空，适用于不需要标签的表单项
- 优先使用组件库提供的布局能力，而不是自己写样式

## 发现问题时的处理流程

1. **识别**：发现现有代码存在坏味道或可优化点
2. **分析**：思考更好的实现方式
3. **提出**：向用户说明问题和改进方案
4. **确认**：获得用户确认后实施改进

## 需要主动思考的场景

- 复制现有代码模式时：这个模式真的是最佳实践吗？
- 添加额外样式时：是否因为组件使用不当才需要这个样式？
- 结构不一致时：是否应该统一处理方式？
- 代码重复时：是否应该抽取公共组件或函数？
