---
description: 前端项目约定 - 代码规范和最佳实践
globs:
  - "front/**/*.vue"
  - "front/**/*.ts"
  - "front/**/*.tsx"
alwaysApply: true
---

# 前端项目约定

## 1. Vue 单文件组件结构

Vue 单文件组件 (.vue) 必须按照以下顺序定义代码块：

1. `<script>` 或 `<script setup>`
2. `<template>`
3. `<style>`

```vue
<script setup lang="ts">
import { ref } from 'vue';

const count = ref(0);
</script>

<template>
  <div>{{ count }}</div>
</template>

<style scoped lang="scss">
div {
  color: #333;
}
</style>
```

---

## 2. bk-table 使用规范

### 2.1 Column render 函数

在 `bk-table-column` 的 `render` 函数中，**必须使用 `row` 而不是 `data`** 来访问行数据。

**原因**：`data` 参数是非响应式的，`row` 参数是响应式的。

```typescript
// ❌ 错误：使用 data
const columnConfig = {
  name: {
    render: ({ data }) => data.name,
  },
};

// ✅ 正确：使用 row
const columnConfig = {
  name: {
    render: ({ row }: { row?: IDataItem }) => row.name,
  },
};
```

### 2.2 表格选择功能

使用 `useTableSelection` hook 处理表格多选，而不是手写选择逻辑。

```typescript
// ❌ 错误：手写选择逻辑
const selectedItems = ref([]);
const handleSelectionChange = ({ checked, row, isAll }) => {
  if (isAll) {
    selectedItems.value = checked ? tableData.value : [];
  } else if (row) {
    // ... 手动处理
  }
};

// ✅ 正确：使用 useTableSelection
import useTableSelection from '@/hooks/use-table-selection';

const isRowSelectable = ({ row }) => {
  return row.status === 'init'; // 根据业务逻辑判断
};

const {
  selections,           // 已选择的项
  checked,              // 已选择项的 id 列表（用于表格默认勾选）
  handleSelectChange,   // 单个选择事件处理
  handleSelectAll,      // 全选事件处理
  resetSelections,      // 重置选择
} = useTableSelection({ isRowSelectable });
```

**模板绑定**：
- `:is-row-select-enable` - 控制行 checkbox 禁用状态（与 hook 的 isRowSelectable 使用同一个函数）
- `@select-all` 和 `@selection-change` - 绑定 hook 返回的事件处理函数

```vue
<bk-table
  :data="tableData"
  :is-row-select-enable="isRowSelectable"
  @select-all="handleSelectAll"
  @selection-change="handleSelectChange"
/>
```

**注意**：`isRowSelectable` 同时传递给 hook 和 table，确保选择逻辑和 UI 禁用状态一致。

---

## 3. bkui-vue3 组件使用

### 3.1 查阅文档的场景

使用 bkui-vue3 组件时，以下场景应主动查阅 MCP 文档：

- **配置属性**：不确定是否有对应的 prop
- **复杂组件**：bk-table、bk-form、bk-tree、bk-dialog 等
- **事件和插槽**：确认名称和参数
- **不确定的用法**：可能有更好的内置方案

### 3.2 查阅方式

```
// 使用 bkui-vue3 MCP 工具
get-component { name: "form" }
get-components-batch { names: ["form", "input", "select"] }
```

### 3.3 实践建议

1. **宁可多查一次，不要猜错实现**
2. **发现组件能力不足时，先确认是否遗漏了内置功能**
3. **避免不必要的样式覆盖**

---

## 4. CSS 类名命名规范

使用**连字符（kebab-case）**命名，不使用 BEM 风格（`__`、`--`）。

```scss
// ❌ 错误：BEM 风格
.resource-entry {
  &__sidebar { ... }
  &__header { ... }
  &__content { ... }
}

// ✅ 正确：连字符风格
.resource-entry { ... }
.resource-entry-sidebar { ... }
.resource-entry-header { ... }
.resource-entry-content { ... }
```

**要点**：
- 组件根元素和子元素统一使用连字符连接
- 通过组件名前缀实现作用域隔离（配合 `scoped`）
- 状态类使用 `is-` 前缀，如 `is-active`、`is-sticky`

---

## 5. 禁止使用工具类 class 设置间距

不使用 `.mt24`、`.mb16`、`.pl8` 等独立的工具类 class 来设置间距。间距应通过语义化的样式规则或行内样式处理。

```scss
// ❌ 错误：定义独立的工具类
.mt24 {
  margin-top: 24px;
}
.mb16 {
  margin-bottom: 16px;
}
```

```vue
<!-- ❌ 错误：使用工具类 -->
<div class="section-title mt24">资源状态</div>

<!-- ✅ 正确：行内样式（适用于一次性间距） -->
<div class="section-title" style="margin-top: 24px">资源状态</div>

<!-- ✅ 正确：语义化 class 中包含间距（适用于可复用场景） -->
<div class="resource-section-title">资源状态</div>
```

```scss
// ✅ 正确：间距作为语义化样式的一部分
.resource-section-title {
  font-size: 14px;
  font-weight: 700;
  margin-top: 24px;
}
```

**原因**：工具类 class 缺乏语义、不易维护，且在 scoped 样式中容易产生冲突。

---

## 6. 响应式数据传递

传递 reactive 对象给外部函数时，应创建副本而非传递引用。

```typescript
// ❌ 错误：传递 reactive 引用
await submitHandler(formModel);
resetForm(); // 可能影响已传递的数据

// ✅ 正确：传递副本
await submitHandler({
  field1: formModel.field1,
  field2: formModel.field2,
});
resetForm(); // 不影响已传递的数据
```
